import { notFound } from 'next/navigation'

// Full experience page — Day 4+ implementation
// Placeholder returns 404 so internal links don't break the build
export default function GitaSlugPage() {
  notFound()
}
